
BASEDIR=$(dirname $0)
RRDTOOL=$BASEDIR/../src/rrdtool

function fail {
	 echo >&2 "FAILED:" "$@"
	 exit 1
}

function ok {
	 echo >&2 "OK:" "$@"
}

function report {
	RC=$?
	if [ "$RC" = 0 ] ; then
		ok "$@"
	else
		fail "$@"
	fi
}
